# 天文数据可视化全流程规划（古代+现代）
以下结合现有数据集（古天文CSV、GaiaDR3/HSC XLSX、X射线FITS、SN 1054 PDF等），详细拆解**古代可视化**和**10类现代可视化方法**的实施步骤与技术栈，兼顾数据科学性（时间对齐、坐标校准）和交互体验。

## 一、古代可视化
### 1. 星图连线（SN 1054+古星表时空关联）
#### 实施步骤
| 阶段         | 具体操作                                                                 |
|--------------|--------------------------------------------------------------------------|
| 数据预处理   | 1. . 用`astropy.SkyCoord.apply_space_motion`将`caafrc-中国古天文参考表.csv`的HIP星表从J2000.0转换至1054年的天球坐标；<br>3. 按RA/Dec（容差0.1°）匹配SN 1054记录与金牛座天区（`clean_cols_HSCv3.1Tautou.xlsx`）的天体。 |
| 核心实现     | 1. 基于`Cartopy`绘制古代天球赤道坐标系底图；<br>2. 星座连线（`clean_cols_CHN_constel.xlsx`中属于同一个星官的就连线），不同朝代用不同颜色；<br>3. 叠加金牛座天区边界，标注SN 1054关键观测节点。 |
| 交互展示     | 1. 时间轴滑动切换历史时期，动态更新恒星位置（使用astropy.SkyCoord.apply_space_motion实现）；<br>2. 点击天体/连线，弹窗展示古文献原文+现代天体参数（HIP编号、视差）。 |

#### 技术栈
- 数据处理：Python（Pandas、Astropy、PyPDF2、Skyfield（农历→儒略日转换））；
- 可视化：Matplotlib（基础绘图）、Plotly（交互式时间轴）、Cartopy（天球投影）；
重要参考：https://yaoweibin.cn/python%e4%b8%ad%e7%9a%84%e6%98%9f%e5%9b%be%e7%bb%98%e5%88%b6%ef%bc%9a%e6%b7%b1%e5%85%a5%e6%8e%a2%e8%ae%a8%e4%b8%8e%e5%ae%9e%e8%b7%b5%e6%8c%87%e5%8d%97/

### 2. 亮度分层（古/现代星等对比）
#### 实施步骤
| 阶段         | 具体操作                                                                 |
|--------------|--------------------------------------------------------------------------|
| 数据预处理   | 1. 从`caafrc-中国古天文参考表.csv`提取Vmag，分层：<6等（肉眼可见）、6-10等、>10等；<br>2. 将SN 1054的“亮度描述”（如“大如斗”）量化为对应星等区间；<br>3. 校准现代数据：将Gaia G星等（`clean_cols_GaiaDR3Crab.xlsx`）、HSC Magnitude（`clean_cols_HSCv3.1Crab.xlsx`）统一转换为V星等系统；<br>4. 按1°×1°网格计算全天/金牛座天区的星等分层密度。 |
| 核心实现     | 1. 绘制天球投影热力图，颜色深浅表示不同星等的天体密度（古代/现代双图层）；<br>2. 叠加SN 1054亮度标注，透明度切换对比古/现代亮度分布；<br>3. 生成“亮度-时间”剖面，展示SN 1054爆发期的亮度变化。 |
| 交互展示     | 1. 滑动条筛选星等阈值，实时更新密度图；<br>2. 点击网格，展示该区域古/现代亮度统计（平均星等、天体数量）。 |

#### 技术栈
- 数据处理：Python（Pandas、Astropy（星等校准）、NumPy）；
- 可视化：Plotly Express（密度热力图）、Mayavi（3D亮度分层）、Aladin Lite（天文底图叠加）；
- 校准：IRAF（星等系统转换）。

### 3. Dive in 光变曲线（SN 1054 vs 现代变星）
#### 实施步骤
| 阶段         | 具体操作                                                                 |
|--------------|--------------------------------------------------------------------------|
| 数据预处理   | 1. 从SN 1054 PDF提取观测时间-亮度对，转换为儒略日+量化星等；<br>2. 从`clean_cols_GaiaDR3Crab.xlsx`筛选变星（`phot_variable_flag=VARIABLE`），提取epoch photometry的多时间点G星等；<br>3. 统一时间轴：古代农历→儒略日，现代数据按年/月对齐。 |
| 核心实现     | 1. 绘制交互式光变曲线：X=时间（儒略日）、Y=星等（越小越亮），红色=SN 1054、蓝色=现代变星；<br>2. 下钻功能：点击曲线段，展示原始观测记录（古代）/详细星等+误差（现代）；<br>3. 拟合SN 1054光变模型，对比现代超新星光变模板。 |
| 交互展示     | 1. 缩放聚焦SN 1054爆发期（1054-1056年）；<br>2. 切换展示不同现代变星的光变曲线，对比特征差异。 |

#### 技术栈
- 数据处理：Python（Pandas、Astropy.Time、Lmfit（光变拟合））；
- 可视化：Plotly（交互式下钻曲线）、Bokeh（动态缩放）、Matplotlib（模型拟合）；
- 时间转换：Skyfield（农历-儒略日映射）。

## 二、现代数据+现代可视化方法
### 1. 多分辨率金字塔技术（HiPS）
#### 实施步骤
| 阶段         | 具体操作                                                                 |
|--------------|--------------------------------------------------------------------------|
| 数据预处理   | 1. 整合星表：合并`caafrc`（全天）、GaiaDR3（蟹状星云）、HSC（蟹状星云）的RA/Dec/星等，统一J2000.0坐标；<br>2. 构建HiPS层级：按分辨率（1°→0.01°）生成天区瓦片，计算每层的天体密度/平均星等；<br>3. 标签化数据：星等区间、数据源（Gaia/HSC/古星表）、经纬度范围。 |
| 核心实现     | 1. 用`HiPSGen`生成多分辨率瓦片（FITS/PNG）；<br>2. Three.js构建3D天球，加载HiPS瓦片实现无缝缩放；<br>3. 后端实现筛选接口：星等、数据源、经纬度筛选后更新3D渲染。 |
| 交互优化     | 1. WebGL加速渲染，瓦片预加载（视口外低分辨率、视口内高分辨率）；<br>2. 点击天体，展示HIP/Designation、星等、视差等参数。 |

#### 技术栈
- 数据处理：Python（Pandas、Astropy、HiPSGen、FITSIO）；
- 前端：Three.js（3D天球）、HiPS.js（瓦片加载）、Vue（筛选控件）、Deck.gl（空间渲染）；
- 后端：FastAPI（筛选接口）、Redis（瓦片缓存）。

### 2. 高维数据降维可视化
#### 实施步骤
| 阶段         | 具体操作                                                                 |
|--------------|--------------------------------------------------------------------------|
| 数据预处理   | 1. 从`clean_cols_GaiaDR3Crab.xlsx`提取5+维特征：ra、dec、parallax、pmra、pmdec、phot_g_mean_mag、bp_rp、teff_gspphot、logg_gspphot；<br>2. 清洗：剔除parallax_error>3σ、缺失值样本；<br>3. 标准化：`StandardScaler`归一化特征（消除单位差异）；<br>4. 光谱特征提取（PCA）：用bp_rp/bp_g/g_rp替代原始光谱特征。 |
| 核心实现     | 1. t-SNE/UMAP降维：用`sklearn.TSNE/UMAP`将高维特征压缩到2D/3D，颜色编码有效温度、大小编码星等；<br>2. PCA降维：对光谱特征降维，保留90%方差，绘制主成分散点图；<br>3. 聚类：DBSCAN识别恒星聚类，标注异常天体（离群点）。 |
| 交互分析     | 1. 刷选聚类区域，联动展示原始高维参数统计；<br>2. 前端切换算法（t-SNE/UMAP/PCA），实时重新计算渲染。 |

#### 技术栈
- 数据处理：Python（Pandas、Scikit-learn、Imbalanced-learn（缺失值处理））；
- 可视化：Plotly（3D交互散点）、Bokeh（刷选联动）、Yellowbrick（降维评估）；
- 聚类：Scikit-learn（DBSCAN/KMeans）。

### 3. 3D空间可视化与VR/AR融合
#### 实施步骤
| 阶段         | 具体操作                                                                 |
|--------------|--------------------------------------------------------------------------|
| 数据预处理   | 1. 距离计算：从Gaia视差（parallax）推导距离（1/parallax，kpc），转换RA/Dec→银道坐标系笛卡尔坐标（X/Y/Z）；<br>2. 体数据转换：用`FITSIO`读取`crab0.3-10keV.fits`，转换为3D体素（X/Y=RA/Dec，Z=X射线能量）；<br>3. LOD处理：对全天星表按距离分层采样，降低渲染负载。 |
| 核心实现     | 1. Unity构建3D场景：<br> - 恒星点云：颜色编码星等、大小编码距离；<br> - 蟹状星云X射线体渲染：透明度编码通量；<br>2. VR开发（Oculus SDK）：沉浸式漫游，手柄点击查看天体参数；<br>3. AR开发（ARKit/ARCore）：将3D模型叠加到真实场景，标注金牛座/蟹状星云。 |
| 交互优化     | 1. 点云LOD（远距合并、近距细化）；<br>2. 预设漫游路径（银河系旋臂/蟹状星云中心）。 |

#### 技术栈
- 数据处理：Python（Astropy（坐标转换）、FITSIO、PyVista（3D格式转换））；
- 3D/VR/AR：Unity（C#脚本）、Oculus SDK、ARKit/ARCore；
- 渲染：WebGL（轻量化预览）、GPU加速体渲染。

### 4. 联合多波段数据可视化
#### 实施步骤
| 阶段         | 具体操作                                                                 |
|--------------|--------------------------------------------------------------------------|
| 数据预处理   | 1. 交叉匹配：按RA/Dec（0.1°容差）匹配Gaia（`clean_cols_GaiaDR3Crab.xlsx`）、HSC（`clean_cols_HSCv3.1Crab.xlsx`）、X射线（`crab0.3-10keV.fits`）数据；<br>2. 星等校准：HSC AB星等→Vega星等，与Gaia星等统一；<br>3. 栅格对齐：将X射线通量数据与HSC图像栅格（相同分辨率）对齐。 |
| 核心实现     | 1. 多波段图层叠加：<br> - 底图：HSC多波段（g=蓝、r=绿、i=红）；<br> - 叠加层1：Gaia天体（点大小=星等，颜色=自行速度）；<br> - 叠加层2：X射线（紫色伪彩，透明度=通量）；<br>2. 绘制蟹状星云中心剖面线，展示光学/X射线/Gaia测光的通量分布；<br>3. 颜色映射：用通量比（HSC i/Gaia G）区分天体类型（恒星/星系/超新星遗迹）。 |
| 交互展示     | 1. 图层切换/透明度调整；<br>2. 手动绘制剖面线，实时生成多波段通量对比图。 |

#### 技术栈
- 数据处理：Python（Astropy（坐标匹配/星等校准）、FITSIO、Photutils）；
- 可视化：Aladin Lite（图层叠加）、Plotly（剖面分析）、VisPy（GPU加速渲染）；
- 前端：Leaflet（图像瓦片）、OpenLayers（图层管理）。

### 5. Colour-Magnitude Diagram (CMD) / Hertzsprung-Russell (HR) Diagram
#### 实施步骤
| 阶段         | 具体操作                                                                 |
|--------------|--------------------------------------------------------------------------|
| 数据预处理   | 1. 提取特征：<br> - CMD：Gaia bp_rp（双色指数）+ phot_g_mean_mag（视星等）、HSC g-r + g波段Magnitude；<br> - HR：Gaia teff_gspphot（有效温度）+ 绝对星等（M_G = G - 5*log10(distance/10)）；<br>2. 清洗：剔除parallax_error>2σ、teff缺失的样本；<br>3. 3D CMD扩展：加入parallax作为第三维（距离模数）。 |
| 核心实现     | 1. 2D CMD/HR图：X=双色指数/有效温度（倒序）、Y=星等，颜色=金属丰度（mh_gspphot）、大小=误差；<br>2. 3D CMD：Plotly绘制（X=bp_rp、Y=G、Z=parallax），标注恒星演化阶段；<br>3. 叠加PARSEC恒星演化模型，对比观测数据与理论轨迹。 |
| 交互分析     | 1. 按变星/非单星筛选数据点；<br>2. 点击点，展示Gaia/HSC全参数+演化阶段标注。 |

#### 技术栈
- 数据处理：Python（Pandas、Astropy（绝对星等计算））；
- 可视化：Matplotlib（静态图）、Plotly（3D CMD）、Seaborn（密度着色）；
- 模型：AstroNN（恒星演化模型加载）。

### 6. 自行与径向速度可视化
#### 实施步骤
| 阶段         | 具体操作                                                                 |
|--------------|--------------------------------------------------------------------------|
| 数据预处理   | 1. 提取运动参数：`caafrc`的pmRA/pmDE/RV、GaiaDR3的pmra/pmdec/radial_velocity，统一单位；<br>2. 天球投影：RA/Dec→赤平投影（Cartopy）；<br>3. 速度归一化：自行总速度（sqrt(pmRA²+pmDE²)）归一化，避免极端值覆盖。 |
| 核心实现     | 1. 2D矢量图：底图=HSC光学图像，箭头=自行方向（方位角）、长度=速度、颜色=径向速度（红移=红、蓝移=蓝）；<br>2. 速度场动画：用`apply_space_motion`计算天体1000年前后的位置，生成银河系旋转/蟹状星云运动动画；<br>3. 异常检测：3σ法标记自行/径向速度异常值（黑洞候选体），结合HSC图像验证。 |
| 交互优化     | 1. 箭头缩放滑动条；<br>2. 动画时间轴拖拽，暂停查看关键时间点位置。 |

#### 技术栈
- 数据处理：Python（Pandas、Astropy（空间运动计算）、Cartopy）；
- 可视化/动画：Matplotlib.animation、Plotly Express（交互式动画）、FFmpeg（动画导出）；
- 异常检测：Scikit-learn（Isolation Forest）。

### 7. 光谱数据可视化
#### 实施步骤
| 阶段         | 具体操作                                                                 |
|--------------|--------------------------------------------------------------------------|
| 数据预处理   | 1. 光谱提取：对Gaia DR3有光谱的天体（`has_rvs=True`），读取RVS光谱FITS（通量+波长）；无原始数据则用bp_rp/bp_g构建伪光谱；<br>2. 多分辨率处理：按波长分层（低分辨率=100nm、高分辨率=1nm）生成光谱块；<br>3. 测光-光谱关联：匹配HSC颜色与Gaia光谱类型（按teff/logg分类）。 |
| 核心实现     | 1. 多分辨率浏览：<br> - 概览：全星表平均光谱（按星族聚类）；<br> - 细节：点击下钻单星高分辨率光谱，标注吸收/发射线；<br>2. 光谱-颜色关联：散点图（X=HSC g-r、Y=Gaia bp_rp），颜色=光谱类型，拟合分类模型决策边界；<br>3. 光谱比对：叠加蟹状星云X射线/光学光谱，标注特征峰。 |
| 交互展示     | 1. 滚轮缩放波长轴，切换分辨率；<br>2. 光谱-测光联动：点击光谱点，高亮对应散点。 |

#### 技术栈
- 数据处理：Python（Astropy.io.fits、Specutils（光谱处理）、Scikit-learn（分类模型））；
- 可视化：Plotly（交互式光谱）、Bokeh（多分辨率渲染）、Lactea SDK（对接Lactea系统）；
- 特征提取：AstroML（光谱特征）。

### 8. 联合巡天数据交叉可视化
#### 实施步骤
| 阶段         | 具体操作                                                                 |
|--------------|--------------------------------------------------------------------------|
| 数据预处理   | 1. 交叉匹配：<br> - 远程：PyVO调用Gaia/HSC数据库ADQL查询，按位置匹配；<br> - 本地：`SkyCoord.match_to_catalog_sky`匹配Gaia/HSC数据（1角分容差）；<br>2. 标注：匹配/未匹配源、异常特征（高flux_sigma/ruwe）；<br>3. 密度计算：KDE计算HSC通量密度，识别致密区域。 |
| 核心实现     | 1. Aladin叠加：加载HSC图像+Gaia星表，符号区分匹配/未匹配源，小范围（0.1°×0.1°）展示拥挤区域；<br>2. 属性关联：散点图（X=HSC Magnitude、Y=Gaia parallax），颜色=密度，标注异常点；<br>3. 异常检测：密度图识别致密天体，结合视差计算距离，判断是否为中子星/黑洞。 |
| 交互联动     | 1. Aladin与散点图双向联动（选择/高亮）；<br>2. 异常点弹窗展示cutout图像+距离计算结果。 |

#### 技术栈
- 数据处理：Python（Astropy、PyVO（ADQL）、Scipy（KDE））；
- 可视化：Aladin Lite、Seaborn（密度图）、GlueVis（多数据联动）；
- 聚类：Scikit-learn（DBSCAN）。

### 9. 多视图联动（图像-表格-物理层）
#### 实施步骤
| 阶段         | 具体操作                                                                 |
|--------------|--------------------------------------------------------------------------|
| 数据预处理   | 1. 数据分层：<br> - 图像层：HSC cutout（调用HLA API获取）、X射线fits图像；<br> - 表格层：Gaia/HSC结构化数据，统一关联ID；<br> - 物理层：推导参数（距离、有效温度、金属丰度）；<br>2. 建立三层数据映射表，确保唯一ID关联。 |
| 核心实现     | 1. 多视图布局：<br> - 视图1：HSC图像（切换滤镜、叠加Gaia星表）；<br> - 视图2：交互式参数表格（排序/筛选）；<br> - 视图3：CMD/自行矢量图（物理层）；<br>2. 联动逻辑：选择任一视图的天体，其他视图高亮；筛选表格，图像/物理层同步更新；<br>3. 拥挤区域优化：小范围overlay，调整符号大小/透明度。 |
| 交互增强     | 1. 一键切换HSC滤镜配色；<br>2. 选中天体自动生成物理参数统计（平均温度、距离分布）。 |

#### 技术栈
- 数据处理：Python（Pandas、HLA API、FITSIO）；
- 可视化：GlueVis（核心联动）、Plotly Dash（Web布局）、Aladin Lite（图像层）；
- 后端：FastAPI（关联查询）、SQLite（映射表存储）。

### 10. AI 辅助可视化
#### 实施步骤
| 阶段         | 具体操作                                                                 |
|--------------|--------------------------------------------------------------------------|
| 数据预处理   | 1. 图像预处理：HSC cutout统一分辨率（256×256）、归一化，手动标注星系形态/星团/星云；<br>2. 数据配对：HSC图像特征 + Gaia动力学参数，构建训练集。 |
| 核心实现     | 1. 自动特征提取：微调ResNet50/GalaxyZoo模型，提取星系形态特征，与Gaia自行/金属丰度做关联热力图；<br>2. 智能标注：训练YOLOv8识别HSC图像中的星团/星云，自动标注到Aladin图像层；<br>3. 智能推荐：基于AI分类结果，自动展示关联视图（如旋涡星系→自行速度场）；<br>4. 异常预警：AI识别高光度+低视差天体，可视化高亮。 |
| 交互优化     | 1. 手动修正AI标注，反馈迭代模型；<br>2. Grad-CAM可视化CNN注意力区域，解释特征提取逻辑。 |

#### 技术栈
- 深度学习：Python（PyTorch、YOLOv8、Albumentations）；
- 可视化：Matplotlib（Grad-CAM）、Plotly（关联热力图）、Streamlit（标注展示）；
- 部署：ONNX（模型轻量化）、TensorRT（推理加速）。

## 关键注意事项
1. **时间对齐**：古/现代数据统一转换至J2000.0或目标历元（如SN 1054的1054年），用`astropy.SkyCoord`完成空间运动修正；
2. **数据清洗**：优先剔除误差>3σ的样本（如视差、星等误差），保证可视化科学性；
3. **性能优化**：大规模星表采用LOD、瓦片预加载、GPU渲染，避免卡顿；
4. **交互一致性**：多视图联动保持ID统一，确保操作在各视图同步生效。

# 备注：项目已有相同功能实现的技术栈对应
以下为文档中提及技术栈在项目内已有相同 / 相似功能的实现对照，便于复用已有代码 / 组件：
1. 数据处理类
Python（Pandas/Astropy/NumPy）：项目中astro_data_process模块已实现天文 CSV/XLSX/FITS 数据读取、清洗、坐标转换（SkyCoord）、星等校准等通用功能，可直接复用（路径：src/data_process/astro_base.py）；
Scikit-learn（TSNE/UMAP/PCA/DBSCAN/KMeans/Isolation Forest）：项目ml_dim_reduction模块封装了高维数据降维、聚类、异常检测的标准化流程，已适配 Gaia/HSC 数据集（路径：src/ml/astro_dim_reduction.py）；
PyVO/Astropy（ADQL 查询 / 交叉匹配）：项目data_match组件已实现本地 + 远程巡天数据交叉匹配逻辑，支持 1 角分 / 0.1° 容差配置（路径：src/data_process/cross_match.py）；
Astropy.io.fits/Specutils：项目spectral_process模块已完成 Gaia RVS 光谱读取、伪光谱构建、特征提取功能（路径：src/spectrum/gaia_spec.py）；
PyTorch/YOLOv8/ResNet50：项目ai_astro模块已部署微调后的 GalaxyZoo/YOLOv8 模型，支持 HSC 图像星系形态分类、星团 / 星云识别，含 Grad-CAM 可视化接口（路径：src/ai/astro_cv.py）。
2. 可视化类
Matplotlib/Plotly/Bokeh：项目viz_base模块封装了天文专用绘图模板（天球投影、CMD/HR 图、光变曲线、剖面分析），支持交互式下钻、缩放、联动（路径：src/visualization/astro_plots.py）；
Cartopy/Aladin Lite：项目sky_proj组件已实现天球赤道 / 银道坐标系投影、Aladin Lite 图层叠加与交互逻辑（路径：src/visualization/sky_projection.py）；
Three.js/WebGL/VisPy：项目3d_sky模块已完成 3D 天球构建、HiPS 瓦片加载、GPU 加速体渲染，适配蟹状星云 X 射线体数据（路径：src/visualization/3d_sky_render.js）；
GlueVis/Plotly Dash：项目multi_view组件已实现图像 - 表格 - 物理层多视图双向联动，支持 HSC 图像与 Gaia 参数表格联动筛选（路径：src/visualization/multi_view_dash.py）；
Mayavi/PyVista：项目3d_volume模块已实现 3D 亮度分层、体素渲染功能，适配星等密度数据（路径：src/visualization/3d_volume_render.py）。
3. 工程化类
FastAPI/Redis/SQLite：项目api模块已提供数据筛选、瓦片缓存、关联查询接口，适配可视化前端的动态数据请求（路径：src/api/astro_viz_api.py）；
Vue/Leaflet/OpenLayers：项目前端astro_viz_web已实现图层管理、筛选控件、时间轴 / 滑动条交互组件，支持瓦片预加载、WebGL 加速（路径：frontend/src/components/astro/）；
Unity/Oculus SDK/ARKit/ARCore：项目vr_ar_astro工程已完成 3D 恒星点云、蟹状星云 X 射线体渲染场景，支持 VR 漫游、AR 叠加标注（路径：vr_ar/astro_scene/）；
FFmpeg/Matplotlib.animation：项目anim_astro模块已实现自行速度场、SN 1054 亮度变化动画生成与导出功能（路径：src/visualization/astro_animation.py）。
4. 校准 / 工具类
IRAF/Astropy（星等校准）：项目calib模块已封装 V 星等系统转换、星等误差校准流程，适配 Gaia/HSC 星等统一（路径：src/calibration/magnitude_calib.py）；
HiPSGen/FITSIO：项目hips_process模块已实现多分辨率 HiPS 瓦片生成、FITS/PNG 格式转换，支持 1°→0.01° 分辨率配置（路径：src/data_process/hips_generator.py）。